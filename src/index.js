import React from 'react';
import ReactDOM from 'react-dom';
import ParenComponent from './components/ParentComponent'

ReactDOM.render(
  <div>
    <ParenComponent />
  </div>,
  document.getElementById('root')
);